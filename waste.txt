// import React, { useState, useEffect, useCallback } from "react";
// import { View, Text, ScrollView, Image, StyleSheet, TouchableOpacity } from "react-native";
// interface Booking {
//     id: number;
//     name: string;
//     date: string;
//     service: string;
//     imageUrl: string;
//     status: string;
// }
// const BookingsList = ({ bookings }: { bookings: Booking[] }) => {
//     const [filteredBookings, setFilteredBookings] = useState([]);
//     const [loading, setLoading] = useState(false);
//     useEffect(() => {
//         setLoading(true);
//         const filtered = bookings.filter((booking) => booking.status === "active");
//         setFilteredBookings(filtered);
//         setLoading(false);
//     }, [bookings]);
//     const handlePress = (booking: any) => {
//         console.log("Booking selected:", booking);
//         // Navigate to details
//     };
//     return (
//         <View style={styles.container}>
//             <Text style={styles.title}>
//                 Active Bookings ({filteredBookings.length})
//             </Text>
//             <ScrollView style={styles.scrollView}>
//                 {filteredBookings.map((booking, index) => (
//                     <TouchableOpacity key={index} onPress={() => handlePress(booking)}>
//                         <View style={styles.bookingCard}>
//                             <Image source={{ uri: booking.imageUrl }} style={styles.image} />
//                             <View style={styles.details}>
//                                 <Text style={styles.name}>{booking.name}</Text>
//                                 <Text style={styles.date}>{booking.date}</Text>
//                                 <Text style={styles.service}>{booking.service}</Text>
//                             </View>
//                         </View>
//                     </TouchableOpacity>
//                 ))}
//             </ScrollView>
//         </View>
//     )
// }
// const styles = StyleSheet.create({
//     container: {
//         flex: 1,
//         padding: 20,
//     },
//     title: {
//         fontSize: 24,
//         fontWeight: "bold",
//         marginBottom: 20,
//     },
//     scrollView: {
//         flex: 1,
//     },
//     bookingCard: {
//         flexDirection: "row",
//         padding: 15,
//         marginBottom: 10,
//         backgroundColor: "#f5f5f5",
//         borderRadius: 8,
//     },
//     image: {
//         width: 60,
//         height: 60,
//         borderRadius: 8,
//         marginRight: 15,
//     },
//     details: {
//         flex: 1,
//     },
//     name: {
//         fontSize: 16,
//         fontWeight: "bold",
//     },
//     date: {
//         fontSize: 14,
//         color: "#666",
//         marginTop: 4,
//     },
//     service: {
//         fontSize: 14,
//         color: "#999",
//         marginTop: 4,
//     },
// });
// export default BookingsList;



Problem -----------------
1.  const [filteredBookings, setFilteredBookings] = useState<Booking[]>([]);
     here not add type
     like -> const [filteredBookings, setFilteredBookings] = useState<Booking[]>([]);

2. do not use index  instead we should have to use  item.id  (because id is unique key)

3. update the state in useEffect which is not a good practice you should have to update the state in other function and that funciton call  in useEffect

4 . use Flatlist instead map because pagination implemented is difficult with map function but in Flatlist it's achive very easily

5  useEffect(() => {
 setLoading(true);
 const filtered = bookings.filter((booking) => booking.status === "active");
 setFilteredBookings(filtered);
 setLoading(false);
 }, [bookings]);
 use useCallback here instead of useEffect because useCallback return memoized callback function which is better for performance optimization